commands:

- init
- update
- patch
- remove


fetchers:

current project
git branch list
git log
d.org issue REST

parsers:

current project (null!) ?!?!
git branch list
git log
d.org issue rest

situation data:
  - git branches
  - git log
  - issue number
  - d.org issue node title & files


waypoint objects:

master branch - aways exists
feature branch - always exists (even if no git branch -- we know it needs to be created)
waypoints x N